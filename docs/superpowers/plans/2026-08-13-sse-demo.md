# SSE 实战 Demo 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 next-demo 中搭建一个可运行的 SSE 学习页(/dashboard/sse-demo),含两个真实 SSE 端点 + 演示页 + 中文指南。

**Architecture:** 两个 Next.js Route Handler 返回 `text/event-stream` 的 `ReadableStream`;一个 `'use client'` 页面用浏览器原生 `EventSource` 订阅,展示实时发票通知与 AI 打字机效果。复用现有 Prisma + shadcn Card/Button/Badge。

**Tech Stack:** Next.js 16 (App Router) / React 19 / Prisma 7 / shadcn-ui / TypeScript / pnpm

## Global Constraints

- **包管理器:严格用 `pnpm`,不要用 npm**(会破坏 .pnpm store);Node 用 `fnm use 20.20.2`。组合切换与命令写在一个 Bash 调用里:`fnm use 20.20.2 && pnpm ...`
- **项目根目录:`/Users/huhejie/CODE/next-demo/next-demo`**(所有相对路径基于此)
- **UI 文本用中文**;不做移动端/响应式,只做桌面
- **类型验证:`pnpm tsc --noEmit` 必须零错误**
- **非 git 仓库**:不执行 `git add`/`git commit`(项目未初始化 git),用 `pnpm tsc --noEmit` + 手动运行作为每个任务的验证关卡
- **TDD 偏离说明**:SSE 端点涉及流/定时器/Prisma,单元测试投入产出比低;且本项目无 git 无法 commit。本计划用「写完整代码 → tsc 验证 → 运行验证」替代 TDD 红/绿/commit 循环。这是基于用户偏好(脚手架式 demo)与项目实情的务实选择。
- **🎯 学习者练习点策略**:计划中所有代码均完整无占位符(满足计划可执行性)。凡标 `// 🎯 学习者练习点` 的行,是 SSE 协议核心知识点;**执行交付时把这些标记处替换为 `// TODO: ...` 中文提示**,让学习者亲手填。计划保留完整版作为参考答案。
- **Invoice 金额单位**:数据库存「分」(int),UI 显示需 `/100` 转元;状态 `paid`→已支付、`pending`→待处理(参考现有 `app/api/invoices/csv/route.ts`)

## File Structure

| 文件 | 职责 | 新建/修改 |
|------|------|----------|
| `app/api/sse/invoices/route.ts` | SSE 端点①:每 4s 查最新 Invoice 并 push,含 id/retry/心跳/断线演示 | 新建 |
| `app/api/sse/ai-stream/route.ts` | SSE 端点②:模拟 AI 打字机,按字 push + `[DONE]` 收尾 | 新建 |
| `app/dashboard/sse-demo/page.tsx` | 演示页('use client'),三区块:发票通知 / AI 流 / 说明卡 | 新建 |
| `docs/13-SSE实战指南.md` | 配套中文讲解:协议要点 + 代码导览 + 坑点 | 新建 |

设计依据:`docs/superpowers/specs/2026-08-13-sse-demo-design.md`

---

## Task 1:SSE 端点① — Invoice 实时通知

**Files:**
- Create: `app/api/sse/invoices/route.ts`
- 参考现有风格: `app/api/invoices/csv/route.ts`(Prisma 用法、Response 风格)

**Interfaces:**
- Consumes: `prisma` 来自 `@/app/lib/prisma`(已存在)
- Produces: `GET /api/sse/invoices` → `text/event-stream`
  - 事件名 `invoice`,数据形如 `{"id","amount","status","date","customerName"}`
  - 每条消息带 `id: <递增序号>`;读请求头 `Last-Event-ID` 让重连后 id 连续(演示 Last-Event-ID 机制)
  - 每 3 条主动 `controller.close()`,触发浏览器自动重连

- [ ] **Step 1:创建端点文件并写入完整代码**

创建 `app/api/sse/invoices/route.ts`:

```ts
// SSE 端点①:实时推送最新发票(模拟"新发票到达"通知)
// 学习重点:SSE 的消息格式(data:/id:/retry:/心跳注释行)、自动重连、Last-Event-ID
import { prisma } from '@/app/lib/prisma';

export async function GET(request: Request) {
  const encoder = new TextEncoder();

  // 读 Last-Event-ID:浏览器断线重连时会自动带上"最后收到的消息 id"
  // 我们从这里继续递增,让 id 连续 —— 演示 Last-Event-ID 机制
  const lastEventId = Number(request.headers.get('Last-Event-ID') || 0);
  let messageId = lastEventId;
  let pushedSinceReconnect = 0;
  // 定时器引用提到外层,以便 cancel() 能清理(客户端断开时防内存泄漏)
  let heartbeat: ReturnType<typeof setInterval>;
  let businessTimer: ReturnType<typeof setInterval>;

  const stream = new ReadableStream({
    async start(controller) {
      // 先推一条 retry,告诉浏览器"断了之后等 3000ms 再连"
      // 🎯 学习者练习点:retry 字段
      controller.enqueue(encoder.encode('retry: 3000\n\n'));

      // 心跳:每 25 秒推一条注释行(: 开头浏览器会忽略)
      // 作用:防止 nginx/CDN 等中间代理因"长时间无数据"掐断连接
      // 🎯 学习者练习点:心跳注释行
      heartbeat = setInterval(() => {
        controller.enqueue(encoder.encode(': ping\n\n'));
      }, 25000);

      // 业务循环:每 4 秒查一次最新发票并推送
      businessTimer = setInterval(async () => {
        try {
          const latest = await prisma.invoice.findFirst({
            include: { customer: true },
            orderBy: { date: 'desc' },
          });

          messageId += 1;
          pushedSinceReconnect += 1;

          const data = {
            id: latest?.id ?? '',
            amount: latest?.amount ?? 0, // 单位:分
            status: latest?.status ?? 'pending',
            date: latest?.date ?? new Date(),
            customerName: latest?.customer?.name ?? '未知客户',
          };

          // 🎯 学习者练习点:SSE 消息格式(id/event/data + \n\n 结束符)
          const payload =
            `id: ${messageId}\n` +
            `event: invoice\n` +
            `data: ${JSON.stringify(data)}\n\n`;
          controller.enqueue(encoder.encode(payload));

          // 断线演示:每推 3 条主动关闭一次
          // 浏览器会自动重连(等 retry 毫秒),并发起新请求(带 Last-Event-ID)
          if (pushedSinceReconnect >= 3) {
            pushedSinceReconnect = 0;
            clearInterval(heartbeat);
            clearInterval(businessTimer);
            controller.close();
          }
        } catch (err) {
          console.error('[sse/invoices] 推送失败:', err);
        }
      }, 4000);
    },
    cancel() {
      // 客户端断开(关页面/source.close())时触发,清理定时器防泄漏
      clearInterval(heartbeat);
      clearInterval(businessTimer);
      console.log('[sse/invoices] 客户端断开连接');
    },
  });

  return new Response(stream, {
    headers: {
      'Content-Type': 'text/event-stream',
      'Cache-Control': 'no-cache, no-transform',
      Connection: 'keep-alive',
    },
  });
}
```

- [ ] **Step 2:tsc 验证零错误**

Run: `cd /Users/huhejie/CODE/next-demo/next-demo && fnm use 20.20.2 && pnpm tsc --noEmit`
Expected: 无输出(零错误)。若报 `ReadableStream` 未定义,说明 tsconfig lib 缺 Web 标准——本项目已含(Next.js 自带),不应出现。

---

## Task 2:SSE 端点② — AI 打字机模拟

**Files:**
- Create: `app/api/sse/ai-stream/route.ts`

**Interfaces:**
- Produces: `GET /api/sse/ai-stream` → `text/event-stream`
  - 连续推 `data: {"delta":"<字>"}\n\n`
  - 结束推 `data: [DONE]\n\n`(致敬 OpenAI 流式格式),随后 `controller.close()`

- [ ] **Step 1:创建端点文件并写入完整代码**

创建 `app/api/sse/ai-stream/route.ts`:

```ts
// SSE 端点②:模拟 AI 流式输出(打字机效果)
// 学习重点:把一段文本切成片段逐个 push,呈现"逐字蹦出"的效果
// 这正是 ChatGPT/Cursor 等 AI 产品"打字机效果"的底层原理

const ANSWER =
  '你好!这是用 SSE 模拟的 AI 流式输出。' +
  '服务器把一段回答切成字符,逐个推送给浏览器,' +
  '客户端每收到一个字符就 append 到界面上,形成打字机效果。' +
  '这也是 ChatGPT 等 AI 产品流式回答的底层原理。';

export async function GET() {
  const encoder = new TextEncoder();
  // 用展开运算符按"字符"切(而非按字节),正确处理中文
  const chars = [...ANSWER];
  let i = 0;
  let timer: ReturnType<typeof setInterval>;

  const stream = new ReadableStream({
    start(controller) {
      timer = setInterval(() => {
        if (i < chars.length) {
          // 🎯 学习者练习点:delta JSON 格式 + \n\n 结束符
          const payload = `data: ${JSON.stringify({ delta: chars[i] })}\n\n`;
          controller.enqueue(encoder.encode(payload));
          i += 1;
        } else {
          // 全部推送完,发 [DONE] 收尾(致敬 OpenAI 流式格式),然后关闭流
          controller.enqueue(encoder.encode('data: [DONE]\n\n'));
          clearInterval(timer);
          controller.close();
        }
      }, 50);
    },
    cancel() {
      // 客户端中途断开时清理,避免定时器继续空跑
      clearInterval(timer);
    },
  });

  return new Response(stream, {
    headers: {
      'Content-Type': 'text/event-stream',
      'Cache-Control': 'no-cache, no-transform',
      Connection: 'keep-alive',
    },
  });
}
```

- [ ] **Step 2:tsc 验证零错误**

Run: `cd /Users/huhejie/CODE/next-demo/next-demo && fnm use 20.20.2 && pnpm tsc --noEmit`
Expected: 无输出(零错误)。

---

## Task 3:演示页 — /dashboard/sse-demo

**Files:**
- Create: `app/dashboard/sse-demo/page.tsx`
- 使用组件: `Card/CardHeader/CardTitle/CardDescription/CardContent`、`Button`、`Badge`(均来自 `@/components/ui/*`,已存在)

**Interfaces:**
- Consumes:
  - `GET /api/sse/invoices` → 命名事件 `invoice`,数据 `{id,amount,status,date,customerName}`
  - `GET /api/sse/ai-stream` → 默认事件,`data` 为 `{"delta":"字"}` 或字符串 `[DONE]`
- Produces: 可访问页面 `/dashboard/sse-demo`(继承 dashboard 布局)

**注意(规避已知坑):**
- `'use client'` 不能与 `async` 共存 → 本页面同步,数据走 EventSource
- React 19 StrictMode 下 `useEffect` 执行两次 → 用 `useRef` 持有 EventSource 防止开两条连接
- `useEffect` 必须 `return () => source.close()` 清理

- [ ] **Step 1:创建演示页文件并写入完整代码**

创建 `app/dashboard/sse-demo/page.tsx`:

```tsx
'use client';

// SSE 实战演示页
// 三个区块:① 实时发票通知(端点①) ② AI 流式回答(端点②) ③ 概念说明
//
// 关键 React 处理:
// - 'use client':EventSource 是浏览器 API,只能在客户端跑
// - useRef 持有 EventSource:防止 React 19 StrictMode 下 useEffect 执行两次开两条连接
// - useEffect return 里 source.close():组件卸载必须关连接,否则内存泄漏

import { useEffect, useRef, useState } from 'react';
import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
} from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { Badge } from '@/components/ui/badge';

// ---------- 工具:金额分→元,状态中文化(复用项目约定) ----------
const formatAmount = (cents: number) => `$${(cents / 100).toFixed(2)}`;
const formatStatus = (s: string) =>
  s === 'paid' ? '已支付' : s === 'pending' ? '待处理' : s;

// ---------- 类型 ----------
type InvoiceMsg = {
  id: string;
  amount: number;
  status: string;
  date: string | Date;
  customerName: string;
};

// ---------- 连接状态徽章 ----------
function StatusBadge({ readyState }: { readyState: number }) {
  // 0=CONNECTING 1=OPEN 2=CLOSED
  const map = {
    0: { text: '连接中…', variant: 'secondary' as const },
    1: { text: '已连接', variant: 'default' as const },
    2: { text: '已断开', variant: 'outline' as const },
  };
  const { text, variant } = map[readyState as 0 | 1 | 2] ?? map[0];
  return <Badge variant={variant}>{text}</Badge>;
}

// ============================================================
// 区块 1:实时发票通知(订阅 /api/sse/invoices)
// ============================================================
function InvoiceStream() {
  const sourceRef = useRef<EventSource | null>(null);
  const [invoices, setInvoices] = useState<InvoiceMsg[]>([]);
  const [readyState, setReadyState] = useState(0);
  const [lastId, setLastId] = useState<number | null>(null);

  useEffect(() => {
    // 防止 StrictMode 双连接:已存在则不再建
    if (sourceRef.current) return;

    const source = new EventSource('/api/sse/invoices');
    sourceRef.current = source;

    // 监听命名事件 invoice(服务端用 event: invoice 发送)
    // 🎯 学习者练习点:命名事件监听 + 数据解析
    source.addEventListener('invoice', (e) => {
      const msg: InvoiceMsg = JSON.parse((e as MessageEvent).data);
      setInvoices((prev) => [msg, ...prev].slice(0, 8)); // 只保留最近 8 条
      setLastId(Number((e as MessageEvent).lastEventId));
    });

    source.onopen = () => setReadyState(source.readyState);
    source.onerror = () => setReadyState(source.readyState);

    // 用轮询更新状态(因为 close()/重连时 onopen/onerror 不一定触发)
    const stateTimer = setInterval(() => {
      if (sourceRef.current) setReadyState(sourceRef.current.readyState);
    }, 500);

    return () => {
      clearInterval(stateTimer);
      source.close();
      sourceRef.current = null;
    };
  }, []);

  return (
    <Card>
      <CardHeader>
        <div className="flex items-center justify-between">
          <div>
            <CardTitle>实时发票通知</CardTitle>
            <CardDescription>
              订阅 <code>/api/sse/invoices</code> · 每 3 条自动断线演示重连
            </CardDescription>
          </div>
          <StatusBadge readyState={readyState} />
        </div>
      </CardHeader>
      <CardContent className="space-y-2">
        <p className="text-sm text-muted-foreground">
          最后消息 id:{lastId ?? '—'}(断线重连后 id 连续,体现 Last-Event-ID)
        </p>
        {invoices.length === 0 && (
          <p className="text-sm text-muted-foreground">等待推送…</p>
        )}
        {invoices.map((inv, idx) => (
          <div
            key={`${inv.id}-${idx}`}
            className="flex items-center justify-between rounded border p-2 text-sm"
          >
            <span>{inv.customerName}</span>
            <span>{formatAmount(inv.amount)}</span>
            <Badge variant="outline">{formatStatus(inv.status)}</Badge>
          </div>
        ))}
      </CardContent>
    </Card>
  );
}

// ============================================================
// 区块 2:AI 流式回答(订阅 /api/sse/ai-stream)
// ============================================================
function AiStream() {
  const sourceRef = useRef<EventSource | null>(null);
  const [text, setText] = useState('');
  const [running, setRunning] = useState(false);
  const [done, setDone] = useState(false);

  const start = () => {
    if (sourceRef.current) return; // 防重复开
    setText('');
    setDone(false);
    setRunning(true);

    const source = new EventSource('/api/sse/ai-stream');
    sourceRef.current = source;

    // 🎯 学习者练习点:onmessage 解析 delta + 处理 [DONE]
    source.onmessage = (e) => {
      if (e.data === '[DONE]') {
        source.close();
        sourceRef.current = null;
        setRunning(false);
        setDone(true);
        return;
      }
      const { delta } = JSON.parse(e.data);
      setText((prev) => prev + delta);
    };

    source.onerror = () => {
      source.close();
      sourceRef.current = null;
      setRunning(false);
    };
  };

  // 卸载清理
  useEffect(() => {
    return () => {
      sourceRef.current?.close();
      sourceRef.current = null;
    };
  }, []);

  return (
    <Card>
      <CardHeader>
        <CardTitle>AI 流式回答</CardTitle>
        <CardDescription>
          订阅 <code>/api/sse/ai-stream</code> · 模拟打字机效果
        </CardDescription>
      </CardHeader>
      <CardContent className="space-y-3">
        <Button onClick={start} disabled={running}>
          {running ? '生成中…' : '生成回答'}
        </Button>
        <div className="min-h-[80px] rounded border bg-muted/30 p-3 text-sm leading-relaxed">
          {text || (done ? '' : '点击「生成回答」开始…')}
          {running && <span className="animate-pulse">▌</span>}
        </div>
      </CardContent>
    </Card>
  );
}

// ============================================================
// 区块 3:概念说明
// ============================================================
function ConceptCard() {
  const items = [
    ['心跳', '注释行 : ping,防止代理超时断连'],
    ['自动重连', '浏览器断线后自动重连,无需手写'],
    ['Last-Event-ID', '重连时浏览器带上最后 id,服务端可补发'],
    ['单向推送', '只能服务器→客户端,发消息需另开 fetch'],
  ];
  return (
    <Card>
      <CardHeader>
        <CardTitle>本页演示的概念</CardTitle>
        <CardDescription>对照代码理解 SSE 四个核心机制</CardDescription>
      </CardHeader>
      <CardContent className="space-y-2">
        {items.map(([k, v]) => (
          <div key={k} className="text-sm">
            <span className="font-medium">{k}:</span>{' '}
            <span className="text-muted-foreground">{v}</span>
          </div>
        ))}
      </CardContent>
    </Card>
  );
}

// ============================================================
// 页面主体
// ============================================================
export default function SseDemoPage() {
  return (
    <div className="grid gap-4 md:grid-cols-2">
      <InvoiceStream />
      <div className="space-y-4">
        <AiStream />
        <ConceptCard />
      </div>
    </div>
  );
}
```

- [ ] **Step 2:tsc 验证零错误**

Run: `cd /Users/huhejie/CODE/next-demo/next-demo && fnm use 20.20.2 && pnpm tsc --noEmit`
Expected: 无输出(零错误)。常见报错排查:
- `Badge variant "secondary"/"outline"` 类型不匹配 → 确认 `components/ui/badge.tsx` 的 `badgeVariants` 含这些 variant(shadcn 默认含 `default/secondary/destructive/outline`)
- `MessageEvent` 类型 → 已用类型断言 `(e as MessageEvent)`

- [ ] **Step 3:运行验证(手动)**

Run: `cd /Users/huhejie/CODE/next-demo/next-demo && fnm use 20.20.2 && pnpm dev`
访问 `http://localhost:3000/dashboard/sse-demo`(需先登录)。
Expected:
- 区块①:约 4s 出现第一条发票;每 3 条后状态徽章短暂变「连接中…」再回「已连接」(自动重连);最后消息 id 递增且重连后连续
- 区块②:点「生成回答」后文字逐字蹦出,结束光标消失
- 区块③:四个概念静态展示

---

## Task 4:配套中文指南

**Files:**
- Create: `docs/13-SSE实战指南.md`

- [ ] **Step 1:创建指南文档**

创建 `docs/13-SSE实战指南.md`(贴合现有 `docs/0X-*.md` 编号风格):

````markdown
# 13. SSE 实战指南

> 本文配合 `/dashboard/sse-demo` 演示页,讲解 Server-Sent Events 的核心机制。

## 什么是 SSE

SSE = 服务器通过一条「不结束的 HTTP 响应」单向推送数据给浏览器。

- 基于 HTTP,不需要新协议(对比 WebSocket)
- 单向:服务器 → 客户端
- 浏览器原生 `EventSource` API 订阅

## 协议三要素

看 `app/api/sse/invoices/route.ts`:

1. **响应头**:`Content-Type: text/event-stream`
2. **消息格式**:`data: <内容>\n\n`(`\n\n` 是消息结束符,缺了浏览器不触发 onmessage)
3. **可选字段**:`id:`(消息id)、`event:`(命名事件)、`retry:`(重连间隔)

## 四个核心机制

| 机制 | 在哪看 | 说明 |
|------|--------|------|
| 心跳 | `: ping\n\n` | 防 nginx/CDN 超时断连 |
| 自动重连 | 断线演示每 3 条 close() | 浏览器自动重连,无需手写 |
| Last-Event-ID | 读 `Last-Event-ID` header | 重连带上最后 id,服务端可补发 |
| 命名事件 | `event: invoice` | 前端 `addEventListener('invoice', fn)` 分流 |

## 前端怎么订阅

```js
const source = new EventSource('/api/sse/invoices')
source.addEventListener('invoice', (e) => {
  const data = JSON.parse(e.data)
})
// 组件卸载必须 close!
source.close()
```

## React 注意事项

- `'use client'`:EventSource 只能客户端跑
- `useRef` 持有 source:防 StrictMode 双连接
- `useEffect` return 里 close():防内存泄漏

## 限制

- 浏览器同域连接数上限(HTTP/1.1 约 6 个)
- 只能 GET,不能自定义 header(鉴权用 Cookie/query)
- 只能传文本
- Serverless 环境会被限时杀掉,仅适合长连接服务器
````

- [ ] **Step 2:无需 tsc**(纯文档)

---

## Task 5:整体验证

- [ ] **Step 1:全量 tsc**

Run: `cd /Users/huhejie/CODE/next-demo/next-demo && fnm use 20.20.2 && pnpm tsc --noEmit`
Expected: 无输出(零错误)。

- [ ] **Step 2:整页运行验证**

Run: `pnpm dev`,登录后访问 `/dashboard/sse-demo`。
Expected: Task 3 Step 3 的全部现象正常;断线重连后发票列表继续累加、id 连续;AI 打字机完整输出结尾文字。

- [ ] **Step 3:把 🎯 学习者练习点替换为 TODO(交付准备)**

执行交付阶段:把三个文件中所有 `// 🎯 学习者练习点` 标记处的**下一行实现代码**替换为 `// TODO: <中文提示>`,保留标记注释本身作为提示。具体替换点:
1. `invoices/route.ts`:`retry` 推送行、心跳行、消息格式拼装行(3 处)
2. `ai-stream/route.ts`:delta payload 拼装行(1 处)
3. `page.tsx`:`addEventListener('invoice')` 内解析、`onmessage` 内解析(2 处)

替换后再次 `pnpm tsc --noEmit` 确认 TODO 版本仍可编译(注释不影响 tsc),但运行时功能缺失——这正是留给学习者填的练习。
